Multi Class Classfication
Activation Function = Softmax
Loss Function = Categorical crossentropy


기술들이 발전하면 모듈이 되고 모듈이 모여서 프레임이 된다.

최종적으로 46개 출력물중에 1개의 벡터가 선택이된다.

validation data에 대한 batch size도 따로 설정할수있다.

학습용, 검증용, 시험용 데이터로 나누는 게 좋지만
서로 겹치지 않는 것이 좋다.


K-Fold Validation을 이용한 Regression
폴드 4개 구성, 데이터 4개로 나누어
1개 폴드 사이즈는 101개, 폴드 시작점은 0번 시작 끝점은 100번

# 시작점 만들기, 끝 점 만들기(검증용 데이터 쪼개기)
# 검증용 라벨데이터 만들기

데이터 4조각 나누기

파라미터 개수를 통해서 모델의 복잡도를 확인할 수 있다
같은 성능의 모델 비교 시 파라미터 개수가 적은 모델이 시스템의 부하를 덜 일으킨다.

CNN
이미지 인식을 위해 패턴찾는데 특히 유용하다

Convolution : 합성곱
필터를 적용시킨다. (특정한 특징을 잡아내고자 할 때)
데이터에서 특징을 뽑아내기 위해 필터를 사용한다.
필터의 패턴은 기본값으로 사용하면 1과 0이 교차되있는 형태의 필터를 사용하게된다.

padding
input_shape
activation

필터링을 위한 필터 사이즈가 같다면 줄이거나 필터링을 못하는 상태가 된다


이미지 사이즈가 충분하지 않다면 필터링을 겹쳐서 하는 경우
이미지 사이즈를 가상으로 늘려서 사이즈를 맞춰 필터링 하는 경우
이미지의 디테일한 특성들은 무시하게